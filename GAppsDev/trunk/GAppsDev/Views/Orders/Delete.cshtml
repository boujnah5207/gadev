@{
    ViewBag.Title = Loc.Dic.Delete;
    ViewBag.PageName = "Orders";
    ViewBag.SubPageName = "AllOrders";
}

<h3>@Loc.Dic.ConfirmDelete</h3>

@Html.Action("PartialDetails", new { id = ViewBag.OrderId})

@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="@Loc.Dic.Delete" /> |
        @Html.ActionLink(Loc.Dic.BackToList, "MyOrders")
    </p>
}
