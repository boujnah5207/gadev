@model IEnumerable<DB.Budgets_BasketsToAllocation>

@{
    ViewBag.Title = "Index";
    ViewBag.Title = Loc.Dic.Permissions;
    ViewBag.PageName = "Managment";
    ViewBag.SubPageName = "AllPermissions";
}1

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Budgets_Baskets.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Budgets_Allocations.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Budgets_Baskets.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Budgets_Allocations.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
