@model DB.User

@{
    ViewBag.Title = Loc.Dic.NewUser;
    ViewBag.PageName = "Managment";
    ViewBag.SubPageName = "AllUsers";
}

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>@Loc.Dic.NewUser</legend>

        <div class="editor-label">
            @Loc.Dic.FirstName
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FirstName)
            @Html.ValidationMessageFor(model => model.FirstName)
        </div>
        <br />

        <div class="editor-label">
            @Loc.Dic.LastName
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LastName)
            @Html.ValidationMessageFor(model => model.LastName)
        </div>
        <br />

        <div class="editor-label">
            @Loc.Dic.Email
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Email)
            @Html.ValidationMessageFor(model => model.Email)
        </div>
        <br />

        <div class="editor-label">
            @Loc.Dic.Role
        </div>
        <div class="editor-field">
            @foreach (string roleName in ViewBag.RolesList)
            {
                <input name="roleNames" type="checkbox" value="@roleName" /> <span>@roleName</span> <br />
            }
        </div>
        <br />

        <div class="editor-label">
            @Loc.Dic.ApprovalRoute
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.DefaultApprovalRouteId, (SelectList)ViewBag.RoutesList)
        </div>
        <br />

        <div class="editor-label">
            @Loc.Dic.DisplayLanguage
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.LanguageId, (SelectList)ViewBag.LanguagesList)
        </div>

        <p>
            <input type="submit" value="@Loc.Dic.Submit" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink(Loc.Dic.BackToList, "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
