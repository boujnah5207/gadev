@model IEnumerable<DB.Budgets_PermissionsToAllocation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Budgets_Permissions.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Budgets_ExpensesToIncomes.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Budgets_Permissions.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Budgets_ExpensesToIncomes.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
