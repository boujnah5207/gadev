@model DB.Budgets_PermissionsToAllocation

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Budgets_PermissionsToAllocation</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.BudgetsPermissionsId, "Budgets_Permissions")
        </div>
        <div class="editor-field">
            @Html.DropDownList("BudgetsPermissionsId", String.Empty)
            @Html.ValidationMessageFor(model => model.BudgetsPermissionsId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.BudgetsExpensesToIncomesId, "Budgets_Allocations")
        </div>
        <div class="editor-field">
            @Html.DropDownList("BudgetsExpensesToIncomesId", String.Empty)
            @Html.ValidationMessageFor(model => model.BudgetsExpensesToIncomesId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
